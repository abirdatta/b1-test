spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver

hibernate:
  dialect: org.hibernate.dialect.H2Dialect
  hbm2ddl.auto: create